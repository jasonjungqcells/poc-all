name: locale_de_de
extends: base_residential
description: >
  A German locale, where the decimal separator is a comma. Telemetry formatting is
  hardcoded to Locale.US on the device, so this exposes any client that parses
  device strings using the phone locale.
tags: [locale, boundary, common]
controls:
  sim.locale: de-DE
